import { makeStyles } from "tss-react/mui";

const useStyles = makeStyles()(() => ({
  appContainer: {
    display: "flex",
    height: "100%",
    width: "100%",
  },
}));

export default useStyles;
